<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>{{viewTitle}}</h1>
    <form action="/cust/edit/{{cust._id}}" method="post">
                <div class="form-group">
                    <label>ID</label>
                    <input disabled type="text" name="id" class="form-control" value="{{cust._id}}">
                </div>
                <div class="form-group">
                    <label>Name</label>
                    <input type="text" name="Name" class="form-control" value="{{cust.name}}">
                </div>
                <div class="form-group">
                    <label>Email</label>
                    <input type="email" name="Email" class="form-control" value="{{cust.email}}">
                </div>
                <div class="form-group">
                    <label>state</label>
                    <input type="state" name="state" class="form-control" value="{{cust.state}}">
                </div>
                {{!-- <div class="form-group">
                    <label>Contactno</label>
                    <input type="number" name="Contactno" class="form-control" value="{{cust.Contactno}}">
                </div> --}}
                
                <div class="form-group">
                    <label>City</label>
                    <select name="city" id="city">
                        <option value="Surat"  {{#if "cust.city== 'Surat'"}} selected {{/if}}>Surat</option>
                        <option value="Valsad"  {{#if "cust.city== 'Valsad'"}} selected {{/if}}>Valsad</option>
                        <option value="Vapi" {{#if "cust.city== 'Vapi'"}} selected {{/if}}>Vapi</option>
                    </select>
                </div>
                
                <div class="form-group">
                    <button type="submit" class="btn btn-success">Submit</button>
                </div>
    </form>
</body>
</html>